# OLAB-TR-019 producer validation

Status: **PASS — producer-ready for independent semantic audit**

The validator independently replays the exact 64-shape and 137-source-coordinate
equality proof, all 137 three-line context packets, 128 unflattened MathML
variants, 15 contextual overrides, and four complete formal-object narratives.
It also closes both proof diagrams, all seven proof commands, the immutable
six-file source replay, and the words-only Listen constraint.

The two profile-invalid propositional meanings identified by the initial
independent FAIL are replaced by first-order structure-and-assignment meanings
at exactly three occurrences. The other 62 meanings remain exact to audited
TR-009. The initial FAIL receipt and report are hash-bound and untouched.

Two fresh isolated builds are byte-identical to each other and to the canonical
projection. Fifteen integrity and semantic mutations are rejected after their
manifest wrappers are refreshed. Protected semantic inputs remained byte-
identical. Mutable durable controls are checked for nonmutation during the run
but deliberately excluded from canonical input hashes, so later operational
advancement cannot change semantic build bytes. This is producer
validation only; independent TR-019 semantic approval remains pending.
