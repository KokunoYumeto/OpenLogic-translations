# OLAB-TR-015 producer validation

Result: **PASS_PRODUCER_REPAIR_READY_FOR_INDEPENDENT_SUPERSEDING_REAUDIT**.

The validator closes 10 source files / 666 lines, 332 occurrence-context pairs, 114 expression shapes, 228 native MathML variants, two formal objects, eight references, zero diagrams, and zero exercises. The semantic partition is 92 locally authored records, 18 exact semantic reuses, and four transparently repaired digit-speech records that retain exact upstream identifier, TeX, meaning, and provenance.

All eight frozen independent semantic classes replay as producer-repaired: five quantified membership readings, sixteen nominal atomic contexts, thirty-three noun/frame overrides, seventeen concise Gamma contexts, four words-only digit repairs, and the corrected universal and assignment-sensitive meanings. Two explicit source-prose notes correct the constants-with-arity claim and the example-domain mismatch while immutable source bytes remain unchanged.

The adversarial suite passes 35 tests. Two fresh isolated builds reproduce all 16 producer artifacts byte-for-byte against canonical evidence and against each other. All frozen inputs, including the complete initial independent FAIL bundle, are byte-identical before and after validation.

Four malformed source formula strings remain unchanged and explicitly disclosed in `SOURCE_CORRECTIONS.jsonl`; the two prose-level corrections are separately disclosed in `SOURCE_PROSE_CORRECTIONS.jsonl`. The original two producer findings and eight independent semantic findings are preserved in `VALIDATION_FAILURE_HISTORY.jsonl`.

This is not an independent semantic PASS. Superseding review by the original independent auditor remains required before reader integration. No HTML reader, browser QA, physical assistive-technology test, cumulative integration, authority/control/prior-tranche/reader mutation, Git operation, remote mutation, credential use, or publication occurred.
